import { Order } from './data/models';
//
// console.log('updating createdAtMilliseconds for orders...');
// const orders = Order.all().then((data) => {
//   data.forEach((order) => {
//     const createdDate = order.createdAt || order.ct;
//     order.createdAtMilliseconds = new Date(createdDate).getTime();
//     order.save();
//   });
// });
